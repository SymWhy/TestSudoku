https://www.geeksforgeeks.org/program-sudoku-generator/

function buildGrid() {
    create 3x3 grid populated with 0s or some null placeholder
    a[0,0,0]
    b[0,0,0]
    c[0,0,0]
}

function populateRnd() {
    populates empty grid() with random digits from 1 to 9, not repeating
    set digits[1,2,3,4,5,6,7,8,9]
    a[0] = random in digits
        pop random number from digits (maybe store it somewhere?)
    repeat until grid is full (check for 0s?)
}

function buildBoard() {
    create 3x3 (or more?) empty grid()s
    sudokuGrids[gridA gridB gridC
               gridD gridE gridF
               gridG gridH gridI]
    populateRnd() if sudokuGrids[i] % 2 = 0
    for gridB,gridD,gridF,gridH populateTest()

}

function populateTest() {
    set digits[1,2,3,4,5,6,7,8,9]
    remove digits that exist in neighbor a[] contents
    pick a random digits[]
    pop that digits[] (are we keeping track of these?)
    replace a[i] with random digits[]
    repeat with each 0 and each set
}